services:
  - type: web

    name: ai-study-room-backend

    runtime: python

    buildCommand: |
      pip install .

    startCommand: |
      uvicorn app.main:app --host 0.0.0.0 --port 10000

    envVars:
      - key: PYTHON_VERSION
        value: 3.11